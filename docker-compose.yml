version: '3.0'
services:
  postgres:
    image: postgres
    restart: always
    ports:
      - "5433:5432"
    environment:
      POSTGRES_DB: db
      POSTGRES_USER: USER
      POSTGRES_PASSWORD: PASSWORD
